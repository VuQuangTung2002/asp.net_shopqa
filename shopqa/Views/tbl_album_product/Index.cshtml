@model IEnumerable<shopqa.Controllers.tbl_album_product>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.fk_product_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.c_img)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.status)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.fk_product_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.c_img)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.status)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.pk_album_product_id }) |
                @Html.ActionLink("Details", "Details", new { id=item.pk_album_product_id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.pk_album_product_id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
